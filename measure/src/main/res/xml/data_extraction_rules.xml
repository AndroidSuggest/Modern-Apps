<?xml version="1.0" encoding="utf-8"?>
<!--
  Measure. Audited state: preferences only, via DataStoreUtils into
  filesDir/datastore_default.preferences_pb (MeasureViewModel) — unit selection
  and calibration. No Room database, no file I/O, nothing on external storage.

  A few bytes of settings, no privacy concerns. Back everything up.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="root" path="." />
        <include domain="file" path="." />
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <include domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="root" path="." />
        <include domain="file" path="." />
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <include domain="external" path="." />
    </device-transfer>
</data-extraction-rules>

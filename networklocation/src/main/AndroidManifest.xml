<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.General">

        <!-- The unbundled provider base classes (com.android.location.provider.*) are supplied by
             the platform at runtime, not packaged in this APK. -->
        <uses-library android:name="com.android.location.provider" android:required="false" />

        <!-- Offline geocoder. Bound by the framework via the GeocodeProvider action; becomes THE
             system geocoder once config_geocodeProviderPackageName points here (MAOS overlay). -->
        <service
            android:name=".GeocodeService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.location.service.GeocodeProvider" />
            </intent-filter>
        </service>

        <!-- Status/info screen. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<?xml version="1.0" encoding="utf-8"?>
<!-- Deny-all, matching the app's android:allowBackup="false". App state is order history
     and vendor session data, which should not leave the device via cloud backup or D2D
     transfer. Overrides the permissive default in the shared :library module. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
